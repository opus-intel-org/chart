apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opus-admin-web.fullname" . }}-test-connection"
  labels:
    {{- include "opus-admin-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opus-admin-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
