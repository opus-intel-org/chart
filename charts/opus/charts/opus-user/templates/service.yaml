apiVersion: v1
kind: Service
metadata:
  name: {{ include "opus-user.fullname" . }}
  labels:
    {{- include "opus-user.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opus-user.selectorLabels" . | nindent 4 }}
