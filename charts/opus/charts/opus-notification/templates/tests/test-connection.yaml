apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opus-notification.fullname" . }}-test-connection"
  labels:
    {{- include "opus-notification.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opus-notification.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
