apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opus-web.fullname" . }}-test-connection"
  labels:
    {{- include "opus-web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opus-web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
