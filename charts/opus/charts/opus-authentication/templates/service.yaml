apiVersion: v1
kind: Service
metadata:
  name: {{ include "opus-authentication.fullname" . }}
  labels:
    {{- include "opus-authentication.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opus-authentication.selectorLabels" . | nindent 4 }}
