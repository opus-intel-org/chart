apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "opus-authentication.fullname" . }}-test-connection"
  labels:
    {{- include "opus-authentication.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "opus-authentication.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
